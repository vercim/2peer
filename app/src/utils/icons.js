// Centralized icon sizes (px) — keep UI icons uniform across the app and a
// touch larger so they don't read as too small. Decorative / illustrative
// graphics (the morphic loader, the empty-screen monitor, the ringing phone)
// keep their own larger literal size and are intentionally not listed here.
export const ICON = {
  sm: 14, // inline icons inside compact controls and list rows
  md: 18, // default — toolbar actions, dialog headers, status indicators
};
